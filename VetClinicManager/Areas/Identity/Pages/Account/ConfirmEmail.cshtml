@page
@model ConfirmEmailModel
@{
    ViewData["Title"] = "Email confirmed";
}

<div class="container d-flex align-items-center" style="min-height: 80vh;">
    <div class="row justify-content-center w-100">
        <div class="col-md-5">
            <div class="card border-0 shadow-sm text-center">
                <div class="card-body p-5">
                    <i class="bi bi-patch-check text-success" style="font-size: 3rem;"></i>
                    <h4 class="mt-3">Email confirmed!</h4>
                    <p class="text-muted mt-2">
                        Your email address has been successfully confirmed.
                        You can now log in to your account.
                    </p>
                    <a asp-page="./Login" class="btn btn-primary mt-3">Log in</a>
                </div>
            </div>
        </div>
    </div>
</div>